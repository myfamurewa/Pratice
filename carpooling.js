var carPooling = function(trips, capacity) {
    let currentPassengers = 0
    let starts = []
    let stops = []
    let passengers = []
    for(let i = 0; i < trips.length; i++){
        passengers.push(trips[i][0])
        starts.push(trips[i][1])
        stops.push(trips[i][2])
    }

 
};